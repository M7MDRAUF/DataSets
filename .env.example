# =============================================================================
# CineMatch Environment Configuration
# =============================================================================
# Copy this file to .env and fill in values as needed.
# DO NOT commit .env to version control!
#
# CineMatch Version: 2.1.6
# Created: December 5, 2025
# =============================================================================

# =============================================================================
# TMDB API Configuration (Optional)
# =============================================================================
# NOTE: The current CineMatch implementation uses the public TMDB image CDN
# (image.tmdb.org/t/p/) which does NOT require an API key for poster display.
# 
# API keys are only needed if you want to add features that require
# authenticated TMDB access, such as:
#   - Real-time movie metadata updates
#   - User ratings sync
#   - Movie search via TMDB API
#   - Fetching movie details/credits
#
# Get your API key from: https://www.themoviedb.org/settings/api
# =============================================================================

# TMDB API v3 key (32 character alphanumeric)
# TMDB_API_KEY=

# TMDB API v4 Read Access Token (JWT-style token)
# TMDB_READ_ACCESS_TOKEN=

# =============================================================================
# Application Paths
# =============================================================================
# These can usually be left at defaults

# Path to MovieLens dataset (default: data/ml-32m)
CINEMATCH_DATA_PATH=data/ml-32m

# Path to trained models (default: models)
CINEMATCH_MODELS_PATH=models

# =============================================================================
# Application Settings
# =============================================================================

# Enable debug mode (true/false) - adds extra logging
CINEMATCH_DEBUG=false

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
CINEMATCH_LOG_LEVEL=INFO

# Maximum recommendations per request (default: 100)
CINEMATCH_MAX_RECOMMENDATIONS=100

# =============================================================================
# Security Settings
# =============================================================================

# Verify SHA256 hash of model files before loading (recommended: true)
# Requires models/model_manifest.json to exist
CINEMATCH_ENABLE_MODEL_HASH_VERIFICATION=true

# =============================================================================
# Docker-specific Settings (for containerized deployment)
# =============================================================================
# When running in Docker, you can pass environment variables via:
#   docker run -e TMDB_API_KEY=xxx ...
#   docker-compose with env_file: .env
# =============================================================================
